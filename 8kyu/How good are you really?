def better_than_average(class_points, your_points):
    a = sum(class_points)
    b = len(class_points)    
    class_points = a / b
    if class_points < your_points:
        return True
    else:
        return False
